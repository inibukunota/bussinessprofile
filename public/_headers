# Cloudflare Pages custom headers — cache static assets aggressively,
# leave HTML uncached so content updates show immediately.
/*
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff

/assets/*
  Cache-Control: public, max-age=31536000, immutable
